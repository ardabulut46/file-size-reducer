# requirements.txt

# Core web framework and its compatible dependencies
Flask==2.2.2
Werkzeug==2.2.2
Jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.3

# Production server for Linux (used in Docker)
gunicorn==20.1.0

# Production server for Windows (used for local dev)
waitress==2.1.2

# Image processing library (headless for servers)
opencv-python-headless==4.9.0.80

# Dependency for OpenCV
numpy==1.26.4